#import unicode_utils;

#align(64)
Foo :: struct {
    x: i32,
}


main :: fn() -> i32 {

    x: = 5;

    x \|= bits

    if is_utf_cont(0x80u8) {
        return 1;
    }

    local: Foo = .{};

    return 0;
}
